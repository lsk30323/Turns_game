# CLAUDE.md — 카드게임 멀티 에이전트 설정

이 저장소는 콘솔 Kotlin 카드게임(`PlayableCardGame.kt`)을 안드로이드 앱으로 포팅하는 프로젝트다.
앱 **빌드 명세**는 `HANDOFF_android_cardgame.md`에 있고, **이 문서(CLAUDE.md)는**
(1) 멀티 에이전트 **연구 → 검수 → 수정/구현 워크플로**와 (2) 프로젝트 **상시 가드레일**을 정의한다.

---

## 0. 진실의 원천 (Source of Truth)

- **`PlayableCardGame.kt`가 기준 구현(reference implementation)이다.** 모든 연구·검수는 이 코드에 비추어 판단한다.
- 이 게임은 **의도적으로 단순한 하스스톤 모델**이다 — 상호작용 스택/체인 없음, 효과는 순차 해결. **이 스코프를 벗어나는 제안(예: 유희왕식 체인·우선권 응답창)은 기각 대상이다.**
- 카드 게임은 본질적으로 **규칙 엔진(rules engine)**이다. 상태 관리·이벤트 처리·객체 생명주기가 핵심이며, 이는 백엔드 사고방식과 직결된다.

---

## 1. 멀티 에이전트 구성 (1 주 에이전트 + 5 서브 에이전트)

**워크플로 한 줄 요약** (3단계): 주 에이전트가 5개 연구 서브 에이전트를 (가능하면 병렬로) 띄워 각 시스템 정보를 수집하게 하고(**연구**) → 그 결과가 `PlayableCardGame.kt`에 맞는지 **검수**한 뒤 → 승인된 범위 안에서 `HANDOFF`의 마일스톤대로 **수정/구현**한다.

### 1.1 서브 에이전트 동작 방식 (Claude Code 사실)

- 서브 에이전트는 `.claude/agents/<name>.md` 마크다운 파일로 정의된다(이 저장소에 포함).
- 각자 **독립된 컨텍스트 창**을 갖고, 작업 후 **최종 결과만** 주 에이전트에게 반환한다.
- **세션 시작 시 로드**된다. 에이전트 파일을 수정하면 세션을 재시작해야 반영된다.
- 부모→자식 채널은 **호출 프롬프트 문자열뿐**이다. 주 에이전트는 서브 에이전트를 띄울 때 **`PlayableCardGame.kt` 경로와 "하스스톤 모델 학습용 프로토타입"이라는 맥락을 프롬프트에 반드시 포함**해야 한다.
- 공식 문서: https://code.claude.com/docs/en/sub-agents

### 1.2 주 에이전트 (이 기본 세션) — 오케스트레이터 · 검수자 · 구현자

**역할**: 3단계로 진행한다 — (1) 5개 서브 에이전트로 **연구 수집**, (2) 그 결과가 `PlayableCardGame.kt`에 맞는지 **검수**, (3) 승인된 범위 안에서 **수정/구현**.

#### Phase 1 — 연구 수집
1. **`PlayableCardGame.kt`와 `HANDOFF_android_cardgame.md`를 먼저 정독**한다.
2. 아래 5개 서브 에이전트를 호출한다(**병렬 권장**). 각 호출 프롬프트에 기준 코드 경로와 스코프 맥락("하스스톤 모델 학습용 프로토타입")을 담는다.

#### Phase 2 — 검수(QC)
3. 각 서브 에이전트의 보고서를 받아 `PlayableCardGame.kt`와 대조해 **갭 분석**을 수행한다:
   - ✅ **잘 맞는 부분** (유지)
   - ⚠️ **부족하거나 모범사례와 어긋나는 부분** (개선 후보)
   - 🟦 **학습용으로 의도적으로 단순화한 부분** (스코프 — 추가하지 말 것)
   - 📌 **우선순위 매긴 구체 권고** (지금 vs 나중)
4. 5개 시스템별로 정리한 **통합 검수 보고서**를 `review/playablecardgame-review.md`로 작성한다. 서브 에이전트가 제시한 **출처 URL을 함께 인용**한다.

**검수 원칙**:
- **연구 결과를 맹목적으로 수용하지 말 것.** 모범사례라도 스코프를 넘으면(유희왕식 체인 등) "의도적 제외"로 분류한다.
- 검수의 목표는 **최대주의(maximalism)가 아니라 적합성(fit)**이다.
- 각 권고에는 "왜 `PlayableCardGame`에 필요한가/불필요한가"를 근거로 단다.

#### Phase 3 — 수정/구현 (검수 통과분 반영 + 앱 빌드)
검수 보고서가 나오면, 거기서 도출된 액션을 실제 코드로 옮긴다.

5. **🚦 확인 게이트**: `review/playablecardgame-review.md`의 📌 권고를 "지금 할 일" 목록으로 추려 **사람에게 제시하고 승인(go)을 받은 뒤** 코드 변경을 시작한다. (사람이 이미 진행을 지시했다면 곧바로 진행.)
6. **무엇을 구현하나** (검수 분류를 그대로 따른다):
   - ⚠️ 부족분 중 검수가 **"지금 반영"** 판정한 것 → **구현**
   - ✅ 잘 맞는 부분 → 변경 없음
   - 🟦 **스코프(의도적 단순화)** → **구현하지 않음** (추가 금지)
   - 📌 **"나중" 권고** → `review/backlog.md`에 기록만, 지금 구현 X
7. **어떻게 구현하나**: `HANDOFF_android_cardgame.md`의 **마일스톤 순서**와 **가드레일**을 그대로 따른다. 특히 검수에서 나온 **도메인 개선(존/효과/상태 등)은 HANDOFF의 도메인 이식 단계(마일스톤 2)에 통합**한다 — "이식하면서 동시에 개선 반영".
8. **커밋·검증**: 마일스톤 단위로 커밋, 각 단계 끝에 빌드/테스트 통과 확인, PR에 **"어떤 검수 권고를 반영했는지"**를 명시.
9. **회귀 방지**: 게임 규칙(턴/마나/소환 멀미/반격/탈진/승패)이 바뀌지 않았는지 도메인 단위 테스트로 확인한다.

**구현 원칙**:
- Phase 2에서 **"스코프 밖"으로 분류한 것은 Phase 3에서 절대 구현하지 않는다.** 구현은 검수가 승인한 범위 안에서만.
- 한 번에 모든 걸 바꾸지 말고, **검수 권고 → 마일스톤 매핑 → 단계별 반영** 순으로 작게 진행한다.

### 1.3 서브 에이전트 5종 (`.claude/agents/`)

| # | 파일 | 조사 주제 |
|---|---|---|
| 1 | `zone-system-researcher.md` | 존(Zone) 시스템 |
| 2 | `card-data-effect-script-researcher.md` | 카드 = 데이터 + 효과 스크립트 |
| 3 | `effect-ability-system-researcher.md` | 효과/능력 시스템 |
| 4 | `timing-priority-researcher.md` | 타이밍·우선권(Priority) |
| 5 | `state-serialization-researcher.md` | 상태 관리·직렬화 |

각 서브 에이전트는 **자기 주제만** 인터넷에서 "몽땅" 조사해 구조화된 보고서를 반환하고, `research/<주제>.md`에 저장한다.

---

## 2. 프로젝트 가드레일 (상시 적용)

- **아키텍처 3계층**: domain(순수 Kotlin, `android.*` import 0) / ViewModel / Compose UI. 의존성 방향 **UI → ViewModel → domain**.
- **카드 게임 모델**: 하스스톤식 **순차 해결**. 상호작용 스택/체인 **없음**.
- **효과는 if/else 하드코딩 금지** — 작은 `Effect` 단위를 조합하고 `EventBus` 훅에 등록한다.
- **카드 = 데이터 + 효과** — 카드 추가가 "데이터 한 줄 추가"에 가깝게 유지한다.
- **앱화 변환 규칙**: `println`→로그 콜백 / `readLine`→`GameAction`(명령 패턴) / 가변 상태→불변 UI 스냅샷(`StateFlow`).
- **게임 규칙 보존**: 턴 교대·마나 +1(최대 10)·소환 멀미·반격·탈진·승패 판정은 `PlayableCardGame.kt`와 동일하게.
- **버전 하드코딩 금지** — 최신 안정 버전을 직접 확인 후 적용, PR에 명시.
- **커밋은 마일스톤 단위**, PR에 결정/버전을 적는다.

---

## 3. 폴더 구조

```
.
├─ PlayableCardGame.kt                 # 기준 구현 (Source of Truth)
├─ HANDOFF_android_cardgame.md         # 앱 빌드 명세
├─ CLAUDE.md                           # 이 문서 (에이전트 역할 + 가드레일)
├─ .claude/agents/                     # 서브 에이전트 5종
│  ├─ zone-system-researcher.md
│  ├─ card-data-effect-script-researcher.md
│  ├─ effect-ability-system-researcher.md
│  ├─ timing-priority-researcher.md
│  └─ state-serialization-researcher.md
├─ research/                           # [Phase 1] 서브 에이전트 산출물 (자동 생성)
├─ review/
│  ├─ playablecardgame-review.md       # [Phase 2] 주 에이전트 통합 검수 (자동 생성)
│  └─ backlog.md                       # [Phase 2→3] "나중" 권고 보관 (자동 생성)
└─ app/                                # [Phase 3] 안드로이드 앱 소스 (HANDOFF 마일스톤대로 생성)
```

---

## 4. 사용법 (사람용)

1. `.claude/agents/`와 `CLAUDE.md`, `PlayableCardGame.kt`, `HANDOFF_android_cardgame.md`를 저장소 루트에 둔다.
2. (에이전트 파일을 막 추가/수정했다면) **Claude Code 세션을 재시작**한다.
3. 메인 세션에 이렇게 지시한다 — 예: *"PlayableCardGame.kt를 5개 서브 에이전트로 검수하고, 결과대로 앱 구현까지 진행해줘."*
4. **[Phase 1·2]** 주 에이전트가 5개 리서처를 띄우고(`research/`에 결과 누적), `review/playablecardgame-review.md`를 만든다.
5. **[🚦 확인 게이트]** 주 에이전트가 "지금 반영할 권고" 목록을 제시한다. 검토 후 진행을 승인한다.
6. **[Phase 3]** 승인된 범위만 `HANDOFF`의 마일스톤대로 구현한다(`app/`에 소스 생성, 마일스톤 단위 커밋). "나중" 권고는 `review/backlog.md`에 남는다.

> 검수만 하고 멈추고 싶으면 3번에서 *"구현 말고 검수만 해줘"*라고 지시하면 된다.
